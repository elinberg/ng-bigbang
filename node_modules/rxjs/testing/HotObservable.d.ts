import { Subject } from '../Subject.d';
import { Subscriber } from '../Subscriber.d';
import { Subscription } from '../Subscription.d';
import { Scheduler } from '../Scheduler.d';
import { TestMessage } from './TestMessage.d';
import { SubscriptionLog } from './SubscriptionLog.d';
import { SubscriptionLoggable } from './SubscriptionLoggable.d';
export declare class HotObservable<T> extends Subject<T> implements SubscriptionLoggable {
    messages: TestMessage[];
    subscriptions: SubscriptionLog[];
    scheduler: Scheduler;
    logSubscribedFrame: () => number;
    logUnsubscribedFrame: (index: number) => void;
    constructor(messages: TestMessage[], scheduler: Scheduler);
    _subscribe(subscriber: Subscriber<any>): Subscription<T>;
    setup(): void;
}
